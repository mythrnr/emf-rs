name: "Release"

on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: false

permissions:
  contents: "read"

jobs:
  publish-wasm:
    name: "Build and publish wasm artifacts"
    runs-on: "ubuntu-latest"
    timeout-minutes: 20
    permissions:
      contents: "write"
    steps:
      - uses: "actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1" # v7.0.1
        with:
          persist-credentials: false

      - uses: "cargo-bins/cargo-binstall@e00d2c94cc0067b77737821097a62d91c0301baa" # v1.21.1

      - name: "Install wasm tooling"
        run: "cargo binstall -y --locked wasm-bindgen-cli wasm-opt wasm-pack"

      - name: "Build wasm (full)"
        run: "make wasm"

      - name: "Package wasm artifacts (full)"
        run: |
          tar -czf "emf-wasm-${GITHUB_REF_NAME}.tar.gz" \
            -C wasm/dist \
            emf_wasm_bg.wasm \
            emf_wasm_bg.wasm.d.ts \
            emf_wasm.js \
            emf_wasm.d.ts \
            package.json

      # Minimal build drops the `tracing` feature so the bundle ships
      # without the `tracing-wasm` dependency. Output goes to a separate
      # directory so it does not collide with the full build above.
      - name: "Build wasm (minimal, no tracing)"
        run: "make wasm-minimal"

      - name: "Package wasm artifacts (minimal)"
        run: |
          tar -czf "emf-wasm-minimal-${GITHUB_REF_NAME}.tar.gz" \
            -C wasm/dist-minimal \
            emf_wasm_bg.wasm \
            emf_wasm_bg.wasm.d.ts \
            emf_wasm.js \
            emf_wasm.d.ts \
            package.json

      - name: "Publish to GitHub Releases"
        env:
          GH_TOKEN: "${{ github.token }}"
        run: |
          if ! gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
            gh release create "${GITHUB_REF_NAME}" \
              --title "${GITHUB_REF_NAME}" \
              --generate-notes
          fi
          gh release upload "${GITHUB_REF_NAME}" \
            "emf-wasm-${GITHUB_REF_NAME}.tar.gz" \
            "emf-wasm-minimal-${GITHUB_REF_NAME}.tar.gz" \
            --clobber
